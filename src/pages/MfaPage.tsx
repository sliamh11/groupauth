// Wave 1 will replace this stub with the full MFA challenge (6-slot OTP, Verify, resend timer).
// Do NOT edit the router (App.tsx) — replace this file's content instead.

export function MfaPage() {
  return (
    <div className="text-center">
      <p className="text-sm font-medium text-muted-foreground">[Wave 1] MFA Challenge Page</p>
      <p className="mt-1 text-xs text-muted-foreground">
        6-slot OTP input, Verify button, resend timer, backup code link
      </p>
    </div>
  )
}

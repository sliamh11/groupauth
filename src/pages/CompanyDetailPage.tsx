// Wave 2 will replace this stub with Company Detail (header, stats, AI Insights, Domain Config, members table).
// Do NOT edit the router (App.tsx) — replace this file's content instead.

export function CompanyDetailPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Company Detail</h1>
      <p className="mt-2 text-sm text-muted-foreground">[Wave 2] Header, 3 stats, AI Insights, Domain Config, members table, MFA toggle</p>
    </div>
  )
}

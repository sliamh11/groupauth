// Wave 1 will replace this stub with the full dashboard (StatCards, CompanyCards, RecentActivity).
// Do NOT edit the router (App.tsx) — replace this file's content instead.

export function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Dashboard</h1>
      <p className="mt-2 text-sm text-muted-foreground">[Wave 1] StatCards, CompanyCards, RecentActivity table</p>
    </div>
  )
}

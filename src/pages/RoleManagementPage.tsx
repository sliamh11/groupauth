// Wave 3 will replace this stub with Role Management (role list + detail panel, permission toggles, Create Role dialog).
// Do NOT edit the router (App.tsx) — replace this file's content instead.

export function RoleManagementPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-foreground">Role Management</h1>
      <p className="mt-2 text-sm text-muted-foreground">[Wave 3] Role list, permission toggle groups, company-scope checkboxes, Create Role dialog</p>
    </div>
  )
}

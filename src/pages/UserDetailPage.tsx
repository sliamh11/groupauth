// Wave 2 will replace this stub with User Detail (profile card, Role Assignments, Login History).
// Do NOT edit the router (App.tsx) — replace this file's content instead.

export function UserDetailPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-foreground">User Detail</h1>
      <p className="mt-2 text-sm text-muted-foreground">[Wave 2] Profile card, Role Assignments table, Login History table</p>
    </div>
  )
}

// Wave 1 will replace this stub with the full login form (email/password, Sign in, Forgot password).
// Do NOT edit the router (App.tsx) — replace this file's content instead.

export function LoginPage() {
  return (
    <div className="text-center">
      <p className="text-sm font-medium text-muted-foreground">[Wave 1] Login Page</p>
      <p className="mt-1 text-xs text-muted-foreground">
        Email/password form, Sign in button, Forgot password link
      </p>
    </div>
  )
}
